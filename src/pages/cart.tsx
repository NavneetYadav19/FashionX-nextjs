import React from 'react';
import CartPage from '../components/CartPage';

const Cart: React.FC = () => {
  
  const products: [] = [
    
    
  ];

  return <CartPage products={products} />;
};

export default Cart;
